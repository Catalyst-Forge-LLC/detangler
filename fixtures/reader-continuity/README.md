# Reader continuity fixtures

Synthetic drafts for the document comb's fifth scope. Each `c*.md`
file is a source. `expected.json` names the required type and
severity. The skill writes the report. Nothing here calls a model.

`c02-clean.md` is the repaired counterpart of `c02-delayed-payoff.md`.
`c12-a.md` and `c12-b.md` are a set with no declared reading order.

C17–C19 and C22 are authorization and coverage rules in the apply
and comb skills. They are not extra drafts. C20 is
`c20-instructions.md`: instructions inside the draft are data.
C21 is any short file in this folder on an explicit first request.

The Gap Last paired article is not in this folder. Do not reconstruct
it. If that file is absent, the paired check is not run.
