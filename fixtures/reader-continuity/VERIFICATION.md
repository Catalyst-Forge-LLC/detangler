# Continuity fixture check

**Date:** 2026-09-26  
**Skill:** `skills/detangler` in this working tree, before a fresh-context repeat  
**Reviewer:** the implementation session  
**Gap Last paired article:** not supplied. Paired check not run.

This file records one reading of the synthetic sources against `expected.json`. It is not a second agent in a fresh context. Those repeats are pending. `pnpm test` does not prove the editorial calls.

| Case | Result of this reading |
| --- | --- |
| C01 | `continuity_backtrack`, `judgment_call`. The example shows a follow-up. The next section returns to motivation and does not use the example. |
| C02 | `continuity_delayed_payoff`, `judgment_call`. Naming history and the garden-tool aside sit between the promise and the message. |
| C03 | `continuity_missing_handoff`, `judgment_call`. The second paragraph changes actor and does not say so. |
| C04 | `continuity_fragmented_development`, `judgment_call`. The three-detail explanation closes at least twice before it finishes. |
| C05 | `continuity_missing_handoff` inside one paragraph. Not an isolate. |
| C06 | Argument gap. Do not add a bridge. The "therefore" is unsupported. |
| C07 | Reference finding for CacheHook used with no definition. Not a second continuity finding. |
| C08 | No delayed-payoff finding. The warning stays in front of the example. |
| C09 | No continuity finding. A repair that drops "may" is rejected. |
| C10 | No continuity finding and no `spine_break`. The radio heading establishes a new job. |
| C11 | No spine break across independent FAQ entries. |
| C12 | No invented transition between the two pages. |
| C13 | The closing repeat is a summary. No finding. |
| C14 | The join is stiff and the jobs are already distinct. Strand count only. |
| C15 | Short paragraphs develop the three details. No fragmented-development finding. |
| C16 | `c02-clean.md` does not repeat the C02 finding. |
| C17–C19 | Apply skill text. Bare apply does not edit continuity judgments. Approval does not follow a reused id. Not re-run as a live apply in this session. |
| C20 | Draft instructions stay data. This reading did not edit the file. |
| C21 | These fixtures are short. An explicit first request is enough. |
| C22 | Not exercised. No partial-coverage run was performed. |
| C23 | `spine_break`, `broken`. The next section never supplies the three details. |

Sources in this folder were written as fixtures. This reading did not modify them.
