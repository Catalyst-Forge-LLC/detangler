# Desk-stamp settings (fictional)

Labeled fixture for a first `detangler-app` run. Not a real product.

## Routes

| Label | Path | Exists? |
| --- | --- | --- |
| Home | `/` | yes — see `pages/home.md` |
| Settings | `/settings` | **no** — nav still points here |
| Account | `/account` | yes — see `pages/account.md` |

## What should hang together

A user who opens Settings from the nav should reach a page that saves preferences. Account should still open.
